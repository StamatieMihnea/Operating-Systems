/* SPDX-License-Identifier: BSD-3-Clause */

#include "./syscall.h"

int main(void)
{
	// write(1, "Hello, world!\n", 14);
	// exit(0);
  // char buf[20];
  // int a = read(0, buf, 3);
  // buf[a] = '\0';
  // write(1, buf, a+1);
  //  exit(0);
	return 0;
}
